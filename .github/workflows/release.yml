name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@5ef0c079ce82195b2a36a210272d6b661572d83e # v2.14.2
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create enterprise-readiness skill package
        run: |
          mkdir -p dist-skill
          cp skills/enterprise-readiness/SKILL.md dist-skill/
          cp LICENSE dist-skill/
          [ -d "skills/enterprise-readiness/references" ] && cp -r skills/enterprise-readiness/references dist-skill/
          [ -d "skills/enterprise-readiness/scripts" ] && cp -r skills/enterprise-readiness/scripts dist-skill/
          [ -d "skills/enterprise-readiness/assets" ] && cp -r skills/enterprise-readiness/assets dist-skill/
          [ -d "skills/enterprise-readiness/templates" ] && cp -r skills/enterprise-readiness/templates dist-skill/
          [ -d "skills/enterprise-readiness/examples" ] && cp -r skills/enterprise-readiness/examples dist-skill/
          [ -f "skills/enterprise-readiness/checkpoints.yaml" ] && cp skills/enterprise-readiness/checkpoints.yaml dist-skill/
          cd dist-skill
          zip -r ../enterprise-readiness-skill-${{ steps.version.outputs.version }}.zip .
          tar -czvf ../enterprise-readiness-skill-${{ steps.version.outputs.version }}.tar.gz .

      - name: Create enterprise-readiness plugin package
        run: |
          mkdir -p dist-plugin
          # Include enterprise-readiness skill files
          mkdir -p dist-plugin/skills/enterprise-readiness
          cp skills/enterprise-readiness/SKILL.md dist-plugin/skills/enterprise-readiness/
          [ -d "skills/enterprise-readiness/references" ] && cp -r skills/enterprise-readiness/references dist-plugin/skills/enterprise-readiness/
          [ -d "skills/enterprise-readiness/scripts" ] && cp -r skills/enterprise-readiness/scripts dist-plugin/skills/enterprise-readiness/
          [ -d "skills/enterprise-readiness/assets" ] && cp -r skills/enterprise-readiness/assets dist-plugin/skills/enterprise-readiness/
          [ -d "skills/enterprise-readiness/templates" ] && cp -r skills/enterprise-readiness/templates dist-plugin/skills/enterprise-readiness/
          [ -d "skills/enterprise-readiness/examples" ] && cp -r skills/enterprise-readiness/examples dist-plugin/skills/enterprise-readiness/
          [ -f "skills/enterprise-readiness/checkpoints.yaml" ] && cp skills/enterprise-readiness/checkpoints.yaml dist-plugin/skills/enterprise-readiness/
          cp LICENSE dist-plugin/
          # Include plugin manifest
          [ -d ".claude-plugin" ] && cp -r .claude-plugin dist-plugin/
          cd dist-plugin
          zip -r ../enterprise-readiness-plugin-${{ steps.version.outputs.version }}.zip .
          tar -czvf ../enterprise-readiness-plugin-${{ steps.version.outputs.version }}.tar.gz .

      - name: Create GitHub Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2.5.0
        with:
          files: |
            enterprise-readiness-skill-${{ steps.version.outputs.version }}.zip
            enterprise-readiness-skill-${{ steps.version.outputs.version }}.tar.gz
            enterprise-readiness-plugin-${{ steps.version.outputs.version }}.zip
            enterprise-readiness-plugin-${{ steps.version.outputs.version }}.tar.gz
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
